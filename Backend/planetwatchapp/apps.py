from django.apps import AppConfig
# from .scheduling import schedule_tasks

class planetwatchappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'planetwatchapp'
